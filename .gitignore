# Local env files (may contain secrets)
.env
.env.local

# Rotated client cert family written by `./deploy-heroku.sh
# extract-agent-certs` or `./run-agent.sh refresh-certs`. The
# canonical source is the published xpmile-uniservice image
# (mints fresh CA per build); these files are local cache only.
certs/cloud-issued/
