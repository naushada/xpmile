version: "3.9"

services:

  # ------------------------------------------------------------------
  # MongoDB — persistent store for shipments, accounts, and GridFS files
  # ------------------------------------------------------------------
  mongodb:
    pull_policy: never
    build:
      context: ./docker
      dockerfile: Dockerfile.mongo
    image: xpmile-mongo:latest
    container_name: xpmile-mongo
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USER:-root}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASS:-changeme}
      MONGO_APP_USER: ${MONGO_APP_USER:-xpmile}
      MONGO_APP_PASS: ${MONGO_APP_PASS:-xpmile_pass}
    volumes:
      - mongo-data:/data/db
    ports:
      - "27017:27017"   # remove this line if you don't need direct host access
    networks:
      - xpmile-net
    healthcheck:
      test: ["CMD-SHELL", "mongosh -u $$MONGO_INITDB_ROOT_USERNAME -p $$MONGO_INITDB_ROOT_PASSWORD --eval \"db.adminCommand('ping')\" --quiet"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  # ------------------------------------------------------------------
  # xpmile application (C++ backend + Angular UI served together)
  # ------------------------------------------------------------------
  app:
    pull_policy: never
    build:
      context: .                    # repo root — Dockerfile COPYs backend/ and ui/
      dockerfile: docker/Dockerfile
    image: xpmile:latest
    container_name: xpmile-app
    restart: unless-stopped
    depends_on:
      mongodb:
        condition: service_healthy  # wait until MongoDB is accepting connections
    environment:
      PORT: "${PORT:-8080}"
      ARGS: >-
        --server-worker ${SERVER_WORKERS:-5}
        --mongo-db-name ${MONGO_DB:-xpmile}
        --mongo-db-connection-pool ${MONGO_POOL:-20}
        --mongo-db-uri mongodb://${MONGO_APP_USER:-xpmile}:${MONGO_APP_PASS:-xpmile_pass}@mongodb:27017/${MONGO_DB:-xpmile}?authSource=admin&maxPoolSize=${MONGO_POOL:-20}
    ports:
      - "${HOST_PORT:-8080}:${PORT:-8080}"
    networks:
      - xpmile-net

# ------------------------------------------------------------------
# Named volume for MongoDB data (survives container restarts)
# ------------------------------------------------------------------
volumes:
  mongo-data:

# ------------------------------------------------------------------
# Isolated bridge network — services talk by name (mongodb, app)
# ------------------------------------------------------------------
networks:
  xpmile-net:
    driver: bridge
