xpmile.backend.base-url=http://localhost:8080
server.port=8090
vaadin.launch-browser=false

# On-prem MongoDB — the SSO-config admin view writes the sso_config document
# directly to this database (docs/design/sso/sso-design.md §10).
xpmile.mongo.uri=mongodb://localhost:27017
xpmile.mongo.database=xpmile
