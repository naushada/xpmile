// Minimal environment file. The build uses production: true (set by
// angular.json's production configuration via fileReplacements — left
// empty here for v1 since there is no dev/prod split worth tracking
// in a login-only SPA).
export const environment = {
  production: true,
};
